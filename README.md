This contact form is connected to the firebase realtime databse to store a contact data .
use usestate hooks for storing a data into a firebase.
to run run the project start a devlopment server to use this command in cmd -> npm start
